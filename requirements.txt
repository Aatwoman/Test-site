streamlit>=1.28.0
ezdxf>=1.0.0
shapely>=2.0.0
ortools>=9.0.0
Pillow>=9.0.0
numpy>=1.24.0
pandas>=1.5.0

# PDF plan import (pdf_import.py)
pymupdf>=1.23.0
opencv-python-headless>=4.8.0
pytesseract>=0.3.10
# pytesseract is a wrapper — it also needs the actual Tesseract OCR engine
# installed as a SYSTEM binary. Unlike a proprietary CAD converter, this
# is a standard, freely apt-installable package, so it works via Streamlit
# Community Cloud's packages.txt (see packages.txt in this repo) as well
# as locally (`apt-get install tesseract-ocr`, `brew install tesseract`,
# etc). Without it, PDFs that need the OCR/raster fallback path fail with
# a clear error; PDFs with real vector content (most CAD "export/print to
# PDF" output) don't need it at all — see pdf_import.py's module docstring.
